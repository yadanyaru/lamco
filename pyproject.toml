[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lamco-minimal-anonymous"
version = "0.1.0"
description = "Anonymous minimal reproduction of label-aware margin coreset selection"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]
